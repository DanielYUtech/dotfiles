skip_global_compinit=1 # Skip global compinit to avoid conflicts with zimfw and fix linux startup issues

export DOTFILES_PATH="/home/danielyutech/.dotfiles"
export DOTLY_PATH="$DOTFILES_PATH/modules/dotly"
export DOTLY_THEME="codely"
export ZIM_HOME="$DOTFILES_PATH/shell/zsh/.zim"
